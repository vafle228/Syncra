# Дев-режим против настоящего Rust-ядра: `npm run dev -- --mode tauri`.
# Обычный `npm run dev` по-прежнему идёт против мок-ядра — см. `resolveCoreMode()`
# в `src/core/ipc.ts`. Этот файл запускает `cargo tauri dev` из ../server.
VITE_CORE=tauri
